import chapterOne from './chapter/one_values_and_variables'
import chapterTwo from './chapter/two_control_structures'

window.addEventListener('DOMContentLoaded', () => {
    // new chapterOne().init();
    new chapterTwo().init();
})
